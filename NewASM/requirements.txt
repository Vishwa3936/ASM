# NewASM — Python requirements
# ---------------------------------------------------------------------------
# The NewASM orchestrator (asm.py + asmtool/) uses ONLY the Python standard
# library — nothing in this file is required to RUN the pipeline itself.
#
# What IS listed here is the one Python-based recon tool the pipeline shells out
# to: subdominator (Stage 1, subdomain enumeration). Install it into a dedicated
# virtualenv so it does not collide with system packages — setup.sh creates
# ~/Tools/Subdominator/sub to match the default path in asmtool/config.py.
#
# Every OTHER tool the pipeline uses is a native binary (Go / Rust / C), NOT a
# pip package. Install the whole toolchain in one shot with:  bash setup.sh
# (full walkthrough in SETUP.md):
#   findomain (Rust) · dnsx, httpx, cdncheck, naabu (Go) · rustscan (Rust) · nmap (C)
# ---------------------------------------------------------------------------

subdominator
